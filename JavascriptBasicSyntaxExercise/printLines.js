let entry = ['3','6','5','Stop','4'];

function solve(lines) {
    for (let i = 0; i < lines.length; i++) {

        if (lines[i] !== 'Stop') {
            console.log(lines[i])
        }
        else{
            break;
        }

    }
}
solve(entry);



